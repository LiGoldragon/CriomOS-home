_:

# criomos-home-lib — home-profile-scoped helpers.
# Placeholder. Real helpers will move here as the port progresses
# (e.g. `mkJsonMerge` if it's only used in home modules, fzf colour maps,
# colemak-layer derivations, etc.).

{ }
