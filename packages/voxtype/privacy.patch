diff --git a/src/daemon.rs b/src/daemon.rs
index 5f02f1a..c54bf85 100644
--- a/src/daemon.rs
+++ b/src/daemon.rs
@@ -995,1 +995,1 @@ impl EagerRecording {
-                        tracing::debug!("Chunk {} completed: {:?}", chunk_index, text);
+                        tracing::debug!("Chunk {} completed ({} chars)", chunk_index, text.len());
@@ -1030,1 +1030,1 @@ impl EagerRecording {
-                    tracing::debug!("Chunk {} completed (waited): {:?}", chunk_index, text);
+                    tracing::debug!("Chunk {} completed after wait ({} chars)", chunk_index, text.len());
@@ -1110,1 +1110,1 @@ impl EagerRecording {
-                        tracing::debug!("Tail transcription: {:?}", text);
+                        tracing::debug!("Tail transcription ready ({} chars)", text.len());
@@ -1128,1 +1128,1 @@ impl EagerRecording {
-        tracing::info!("Combined eager transcription: {:?}", combined);
+        tracing::info!("Combined eager transcription ready ({} chars)", combined.len());
@@ -1244,1 +1244,1 @@ impl Daemon {
-                    tracing::info!("Transcribed: {:?}", text);
+                    tracing::info!("Transcription ready ({} chars)", text.len());
@@ -1249,1 +1249,1 @@ impl Daemon {
-                        tracing::debug!("After text processing: {:?}", processed_text);
+                        tracing::debug!("Text processing complete ({} chars)", processed_text.len());
@@ -1260,3 +1260,3 @@ impl Daemon {
-                            "Smart auto-submit triggered, stripped text: {:?}",
-                            processed_text
+                            "Smart auto-submit triggered, stripped text ready ({} chars)",
+                            processed_text.len()
                         );
@@ -1294,1 +1294,1 @@ impl Daemon {
-                            tracing::info!("Post-processed: {:?}", result);
+                            tracing::info!("Post-processing complete ({} chars)", result.len());
@@ -1299,3 +1299,3 @@ impl Daemon {
-                                tracing::info!("Post-processing: {:?}", processed_text);
+                                tracing::info!("Post-processing text ({} chars)", processed_text.len());
                                 let result = post_processor.process(&processed_text).await;
-                                tracing::info!("Post-processed: {:?}", result);
+                                tracing::info!("Post-processing complete ({} chars)", result.len());
@@ -1308,3 +1308,3 @@ impl Daemon {
-                        tracing::info!("Post-processing: {:?}", processed_text);
+                        tracing::info!("Post-processing text ({} chars)", processed_text.len());
                         let result = post_processor.process(&processed_text).await;
-                        tracing::info!("Post-processed: {:?}", result);
+                        tracing::info!("Post-processing complete ({} chars)", result.len());
@@ -1318,3 +1318,3 @@ impl Daemon {
-                            "Smart auto-submit: final text after post-processing: {:?}",
-                            final_text
+                            "Smart auto-submit: final text after post-processing ready ({} chars)",
+                            final_text.len()
                         );
diff --git a/src/transcribe/remote.rs b/src/transcribe/remote.rs
index c8fcaa1..7c5f3d2 100644
--- a/src/transcribe/remote.rs
+++ b/src/transcribe/remote.rs
@@ -251,8 +251,4 @@ impl Transcriber for RemoteTranscriber {
-            "Remote transcription completed in {:.2}s: {:?}",
+            "Remote transcription completed in {:.2}s ({} chars)",
             start.elapsed().as_secs_f32(),
-            if text.chars().count() > 50 {
-                format!("{}...", text.chars().take(50).collect::<String>())
-            } else {
-                text.clone()
-            }
+            text.len()
         );
diff --git a/src/main.rs b/src/main.rs
index bc7f488..b5e5990 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -319,1 +319,4 @@ async fn main() -> anyhow::Result<()> {
     }
+
+    // The key is now copied into config; do not pass it to output helper commands.
+    std::env::remove_var("VOXTYPE_WHISPER_API_KEY");
