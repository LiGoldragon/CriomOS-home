# Toolchain refresh hiccup ledger

This ledger records workflow/documentation friction encountered while refreshing Pi, Codex, and Claude Code on 2026-07-14. It intentionally separates workflow doctrine defects from ordinary package-build outcomes.

| Class | Attempted step | Observed symptom | Resolution / state | Suggested correction |
|---|---|---|---|---|
| Documentation/workflow | Register the assigned session and lane using the coordination example. | The first request was rejected with `unexpected closing delimiter`; the lane record had been closed before the `Fresh` argument. | Retried with `(Register ((Session Lane ([Role] Structural) [detail]) Fresh))`, then claimed the paths successfully. | In the edit-coordination guidance, add a fully concrete valid `Fresh` registration using a bracketed detail string and call out that the lane record closes once before `Fresh`. |
| Documentation/workflow | Follow the feature-worktree instruction using `bd worktree create` for the dirty `CriomOS-home` checkout. | Beads created a Git worktree but no Jujutsu workspace. `jj -R <worktree>` reported that no jj repository existed. `jj git init --colocate` is explicitly rejected inside a Git worktree. | Removed the newly-created empty worktree with Beads' safety override and created the isolated working copy with `jj -R <repo> workspace add --revision main`. The Beads tracking issue remains the required final-disposition record. | Reconcile `feature-development` and edit-coordination doctrine: `bd worktree create` and Jujutsu workspace management are not composable in this setup. Specify one canonical Jujutsu-aware flow and how its Beads redirect/disposition record is established. |
| Documentation/workflow | Evaluate the flake from the isolated workspace under `primary/agent-worktrees`. | Plain `nix eval .#...` treated the enclosing primary Git repository as the source and rejected the nested worktree file as untracked. | Used `path:/home/li/primary/agent-worktrees/ToolchainRefreshHome#...` for lock updates, evaluation, builds, and checks. | Add this nested-worktree Nix invocation to `nix-usage` or feature-worktree guidance, including the reason plain `.#` is unsafe here. |
| Environment/setup | Run the required Beads commands in CriomOS and CriomOS-home. | Every invocation warned that the shared `.beads` directory mode is `0755` rather than the recommended `0700`. | Continued; the warning did not block tracking. | Add a workspace provisioning check/fix for Beads directory permissions, or document who owns the one-time `0700` correction. |
| Tooling concurrency | Query multiple Nix flake surfaces in parallel. | Nix emitted an ignored `eval-cache-v6 ... sqlite ... is busy` diagnostic. | The requested evaluations still completed; subsequent critical builds were run independently. | Document that parallel Nix evaluation can contend on the local eval cache and that pass/fail evidence should use independent commands. |
| Product/build | Build Pi at upstream tag `v0.80.7`. | The pin transition changed the fixed-output npm dependency hash; the prior hash failed with the newly observed content hash. | Updated `npmDepsHash` to the hash reported by Nix. The package and the dedicated package-load check then built successfully. | No doctrine correction: this is the expected fixed-output dependency update workflow. |
| Product/runtime boundary | Invoke the newly built Pi derivation before home activation. | Its wrapper defaults `PI_PACKAGE_DIR` to the mutable managed package directory, which still contained the prior package and made `--version` print `0.80.6`; with an empty override it prints `0.0.0`. | The requested full-system activation reconciled the managed Pi package directory. The deployed profile `pi` and `direct-pi` now both print `0.80.7`. | Document in the Pi package/test guidance that the CLI version is supplied by `PI_PACKAGE_DIR`; standalone derivation checks must provide a representative package directory, while acceptance must invoke the activated profile. |
| Documentation/workflow | Claim the task locator `/home/li/primary/LiGoldragon/skills`. | Orchestrate accepted the claim even though the path did not exist; the real checkout was `/git/github.com/LiGoldragon/skills`. | Released the false claim, resolved the repository, and reclaimed the real checkout. | Require checkout/parent existence verification after registration; state that claim acceptance does not prove filesystem existence. |
| Coordination tooling | Resume the completed lane with `meta-orchestrate "(Register ((PiExtensionForkProtocol AuthorUpdateAndForkReconciliationDoctrine ([SkillEditor] Structural) [review followup for extension fork reconciliation]) Recovery))"`. | Meta returned `RecoveryInherited`, but `orchestrate "(Observe Lanes)"` still showed the lane `Released`; the next claim returned `lane is not registered: AuthorUpdateAndForkReconciliationDoctrine`. | Stopped using the released lane. With Manager approval, registered distinct Fresh follow-up identity `PiExtensionForkProtocolReview` / `AuthorReconciliationEvidence`. | Make Recovery atomic or return a failure instead of `RecoveryInherited`; doctrine now forbids mutating the contradictory lane and requires Manager-approved Fresh follow-up identity. |
| Tooling/evidence | Probe old patches on the update target with ordinary batch patching. | GNU patch can identify a reversed patch and, without `--forward`, assume `-R`; a zero exit can therefore mean the local delta was removed rather than applied. | Re-ran every probe with `patch --dry-run --forward --batch --verbose` and retained per-hunk output. | Fork reconciliation requires forward-only dry runs and treats reversed-patch notices as absorption evidence, never successful rebase evidence. |
| Upstream artifact | Run the upstream package's declared tests from the npm tarball. | The npm artifact retained test scripts in `package.json` but omitted the `test/` tree through its published `files` list. | Used npm `gitHead` to fetch immutable upstream commit `12a157d2a70b2f4cbc004c020c5f9213b6d8eea8`, then ran pristine and reconciled tests from that source tree. | Extension update doctrine now directs workers from incomplete release artifacts to their immutable recorded source revision. |
| Documentation/workflow | Format a Nix file from the nested JJ workspace with `nix fmt`, including an attempted `path:$PWD` argument. | Current `nix fmt` treated the nested file as part of the enclosing primary repository and rejected its flake as untracked; unlike build/eval commands, this interface forwards positional arguments to the formatter rather than accepting a flake locator. | Ran the nested formatter explicitly with `nix shell "path:$PWD#formatter.x86_64-linux" --command nixfmt --check <file>`. | Extend nested-worktree Nix guidance with an explicit formatter invocation rather than suggesting the build/eval `path:` shape works directly with `nix fmt`. |
| Evidence/review | Submit the first retained fork ledger and fixture for independent review. | Review rejected conclusory summaries: commands were not retained as executables, baseline-equivalent red tests did not force every decision provisional, the stderr witness proved helper compaction but was described as a live 64 KiB bound, and the generation test checked substrings rather than delta fields and enums. | Added one executable verifier for applicability, all pristine/reconciled witnesses, focused/full tests, Nix package/content checks, and Pi RPC load; added a Nix test derivation; narrowed stderr to best-effort post-close compaction; made all six decisions provisional; parsed every fixture record and pinned the canonical ledger digest. | Protocol doctrine now requires selected decision, exact witness command/results, final/provisional state, and provisional treatment of every unexecuted or failing gate including baseline-equivalent failures. |
